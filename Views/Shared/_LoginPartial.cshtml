@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager


@if (SignInManager.IsSignedIn(User))
{
    <li dir="rtl">
        <a  class="nav-link text-white" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">مرحبا  @User.Identity?.Name!</a>
    </li>
    <li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button  type="submit" class="nav-link btn btn-link text-white">تسجيل الخروج</button>
        </form>
    </li>
    <li>
        <a class="nav-link text-white" href="/Home/orders">الطلبات </a>
    <li><a href="https://localhost:62599">الادمن</a></li>

    </li>
}
else
{
    <li >
        <a class="nav-link text-white" asp-area="Identity" asp-page="/Account/Register">انشاء حساب</a>
    </li>
    <li >
        <a class="nav-link text-white" asp-area="Identity" asp-page="/Account/Login">تسجيل الدخول </a>
    </li>
}

